# 集群行为、群体智能、集群无人机控制、生物群体仿生学相关分类
categories: 
  - "cs.RO"   # Robotics - 机器人学（集群机器人、无人机、仿生机器人）
  - "cs.SY"   # Systems and Control - 系统与控制（分布式控制、编队控制）
  - "cs.MA"   # Multiagent Systems - 多智能体系统（群体智能核心分类）
  - "cs.AI"   # Artificial Intelligence - 人工智能（群体智能算法、仿生算法）
  - "cs.LG"   # Machine Learning - 机器学习（如果涉及学习型集群算法）
  - "q-bio.PE" # Populations and Evolution - 群体生物学（生物群体行为研究）

# 关键词：集群行为、群体智能、集群控制相关
keywords:
  # 核心术语
  - "swarm"
  - "swarm intelligence"
  - "swarm robotics"
  - "multi-agent"
  - "multiagent"
  - "multi-agent system"
  - "collective behavior"
  - "collective intelligence"
  
  # 集群控制相关
  - "swarm control"
  - "formation control"
  - "consensus"
  - "consensus control"
  - "distributed control"
  - "cooperative control"
  - "flocking"
  - "herding"
  
  # 无人机集群
  - "drone swarm"
  - "UAV swarm"
  - "swarm of drones"
  - "multi-UAV"
  - "multi-drone"
  - "quadrotor swarm"
  - "fleet of drones"
  
  # 集群算法
  - "particle swarm"
  - "ant colony"
  - "bee colony"
  - "swarm optimization"
  - "swarm algorithm"
  - "emergent behavior"
  - "self-organization"
  
  # 生物群体仿生学
  - "bio-inspired"
  - "biomimetic"
  - "biological collective behavior"
  - "animal collective behavior"
  - "biological swarm"
  - "animal swarm"
  - "fish school"
  - "bird flock"
  - "insect swarm"
  - "collective motion"
  
  # 信息传递与集群机理
  - "information transfer"
  - "swarm communication"
  - "collective communication"
  - "pheromone"
  - "stigmergy"
  - "quorum sensing"
  - "collective decision making"
  - "collective sensing"
  - "biological information processing"
  - "swarm intelligence mechanism"
  - "collective mechanism"

logic: "AND"                 # 组间逻辑：categories 与 keywords 间 AND/OR
max_results: 30
sort_by: "lastUpdatedDate"  # 还可以是submittedDate 建议改成lastUpdatedDate按更新时间排，能捞到v2/v3的“新增”
sort_order: "descending"

# zh / en / both
lang: "both"

# —— 摘要设置 ——
# mode: none / heuristic / llm
summary:
  mode: llm # mode: "heuristic"
  # scope: tldr / full / both
  scope: "both"

# —— 邮件发送（QQ 邮箱） ——
email:
  enabled: true
  subject: "[arXiv] 集群智能与群体行为 每日速递"
  smtp_server: "smtp.qq.com"
  smtp_port: 465
  smtp_pass_env: "SMTP_PASS"
  tls: "ssl"  # auto / ssl / starttls
  debug: false     # true 可打印 SMTP 调试日志
  detail: "full"  # simple / full
  max_items: 30
  attach_md: true  # 是否开启Markdown附件
  attach_pdf: false  # 是否开启PDF附件

# —— LLM（DeepSeek/OpenAI 兼容） ——
llm:
  provider: "openai-compatible"     # 兼容 OpenAI SDK 的服务
  # Deepseek的版本（已启用）
  base_url: "https://api.deepseek.com"
  model: "deepseek-chat"
  api_key_env: "OPENAI_COMPAT_API_KEY"
  # 硅基流动的版本（备用）
  # base_url: "https://api.siliconflow.cn"
  # model: "Qwen/Qwen2.5-7B-Instruct"
  # api_key_env: "OPENAI_COMPAT_API_KEY"
  

  system_prompt_en: |
    You are a senior paper-reading assistant. Based on the given metadata and abstract,
    write ONE concise paragraph in English that covers: research motivation, main method,
    and key experimental findings. Be objective and specific; avoid marketing language.
    Preserve any provided links, but do NOT repeat them in the paragraph.
  system_prompt_zh: |
    你是资深论文阅读助手。基于给定的论文元信息与摘要，用中文写「一段」简洁的总结，
    包含研究动机、核心方法与关键实验结果。保持客观、术语准确；避免营销式表述。
    如有链接请保留，但不要在总结里重复链接。

# —— 中文翻译设置（用 LLM 翻译 title/summary） ——
translate:
  enabled: true         # 是否启用
  lang: "zh"            # 目前只需中文；也可扩展 en/ja 等
  fields: ["title", "summary"]   # 可扩：["title","summary","comments"]

site:
  enabled: true
  dir: "docs" 
  title: "集群智能与群体行为 - arXiv 论文速递"
  keep_runs: 1024         # 历史保留期（按页面份数）
  theme: "light"        # "light" | "dark" | "auto"
  accent: "#2563eb"     # 主题色（可自定义）

freshness:
  since_days: 3650           # 只看最近 N 天内新提交/更新（1=近24h）
  unique_only: true       # 开启跨天去重（见过的 arXiv ID 不再重复发）
  state_path: ".state/seen.json"  # 去重状态文件（会随 docs 一起提交保存）
  fallback_when_empty: false # 当今天无新增时，是否回退展示最近的Top若干（建议false）

scrape:
  html: true               # 先用 HTML 页抓链接
  pdf_if_missing: true     # 若没抓到 code，再去扫 PDF 头部兜底
  pdf_first_page: false    # （可保留为 false；若你想“不管有没有都扫 PDF”，设 true）
  timeout: 10
